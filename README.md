Running an experiment is as simple as running

`launch_redwood.sh [instance] [session]` 

Where instance is a reference to 
	campus

and session references the redwood session number you are trying to run.

